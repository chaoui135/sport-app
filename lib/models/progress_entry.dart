// lib/models/progress_entry.dart
class ProgressEntry {
  final double value; // La valeur de la progression (par exemple, poids, calories brûlées)
  final DateTime date; // La date de la progression

  ProgressEntry({required this.value, required this.date});
}
